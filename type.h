#ifndef TYPE_H
#define TYPE_H

// #include <stdio.h>

// --------------------------------------------------------------------------------------------------
typedef char* obj_t;
#define BAD_OBJECT NULL
// --------------------------------------------------------------------------------------------------
struct node_t
{
    node_t* father;
    node_t* left;
    node_t* right;
    obj_t object;
    size_t hash;
};
// --------------------------------------------------------------------------------------------------
struct tree_t
{
    node_t* null;
    size_t size;
    int num_dump;
};
// --------------------------------------------------------------------------------------------------

// --------------------------------------------------------------------------------------------------
enum SIDES
{
    LEFT_SIDE   = 0,
    RIGHT_SIDE  = 1
};
// --------------------------------------------------------------------------------------------------
enum BOOL
{
    USER_YES = 0,
    USER_NO  = 1
};
// --------------------------------------------------------------------------------------------------
enum EVENT
{
    NULL_EVENT          = 0,
    START_PROGRAM       = 1,
    GUESS_OBJECT        = 2,
    COMPARE_OBJECTS     = 3,
    DESC_OBJECT         = 4,
    SAVE_DATA           = 5,
    EXIT_PROGRAM        = 6
};
// --------------------------------------------------------------------------------------------------



#endif // TYPE_H
