#include <stdio.h>

#include "func.h"

int main (void)
{
    akinator ();
    // tree_t tree = {};
    // create_tree (&tree);

    // import_data (&tree);

    // dump_tree (&tree, "test");

    // // for (int i = 0; i < 3; i++)
    // // {
    // //     // // import_data (&tree);
    // //     guess_object (&tree);
    // // }
    // desc_object (&tree);

    // save_data (&tree);

    // // desc_object (&tree);
    // // compare_objects (&tree);

    // delete_tree (&tree);

    return 0;
}
